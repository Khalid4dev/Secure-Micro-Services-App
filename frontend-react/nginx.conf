server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: proxy /api requests to gateway if not using absolute URLs in frontend
    # But we configured frontend to point to http://localhost:9090
}
